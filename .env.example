# Optional. Enables summaries, difficulty analysis, word definitions, quizzes, and insights.
# Leave unset to run Velox as a local-first reading app without AI features.
GEMINI_API_KEY=""

# Optional. Used by hosted deployments for self-referential links.
APP_URL=""

# Optional. Defaults to 3000.
PORT="3001"
